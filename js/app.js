console.log('up and running');

// $('.toggle').on('click', function() {});
// is equivalent of $('.toggle').click(function() {});
$('.cell').on('click', function() {
    $('id').toggleClass('show-list');
    });